version: "3.9"

services:
  db:
    image: mysql:8.0
    container_name: subscribers_mysql
    restart: unless-stopped
    environment:
      MYSQL_ROOT_PASSWORD: Str0ngP@ssw0rd!
      MYSQL_DATABASE: subscribers_db
      MYSQL_USER: dbadmin
      MYSQL_PASSWORD: AdminP@ssw0rd!
    ports:
      - "3306:3306"
    command: ["--default-authentication-plugin=mysql_native_password"]
    volumes:
      - mysql_data:/var/lib/mysql

volumes:
  mysql_data:
